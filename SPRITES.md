# Руководство по использованию спрайтов

## 📚 Популярные источники бесплатных спрайтов

### 1. **Kenney.nl** (Рекомендуется)
- **Сайт**: https://kenney.nl/assets
- **Особенности**: 
  - Огромная коллекция бесплатных спрайтов
  - Отдельный набор для платформеров: "Platformer Pack"
  - Все спрайты в едином стиле
  - Лицензия: CC0 (можно использовать свободно)

### 2. **OpenGameArt.org**
- **Сайт**: https://opengameart.org
- **Особенности**:
  - Большая библиотека открытых артов
  - Разные стили и темы
  - Фильтры по лицензиям

### 3. **itch.io**
- **Сайт**: https://itch.io/game-assets/free
- **Особенности**:
  - Множество бесплатных наборов спрайтов
  - Разные стили (пиксель-арт, векторная графика)
  - Можно найти готовые тайлсеты

### 4. **Craftpix.net**
- **Сайт**: https://craftpix.net/freebies/
- **Особенности**:
  - Бесплатные наборы спрайтов
  - Качественная графика
  - Регистрация может потребоваться для некоторых ресурсов

## 🎮 Как добавить спрайты в игру

### Шаг 1: Скачайте спрайты
1. Скачайте спрайты с одного из указанных сайтов
2. Создайте папку `assets/images/` в корне проекта
3. Поместите изображения спрайтов в эту папку

### Шаг 2: Подготовьте спрайты
- **Одиночные спрайты**: PNG файлы с прозрачностью
- **Спрайт-листы**: Одно изображение с несколькими кадрами анимации
  - Кадры должны быть расположены горизонтально
  - Все кадры должны быть одинакового размера

### Шаг 3: Включите спрайты в игре

Откройте `js/Game.js` и найдите метод `loadSprites()`:

```javascript
async loadSprites() {
    try {
        // Загружаем спрайты
        await this.spriteManager.loadSprite('player', 'assets/images/player.png', 32, 32, 4);
        await this.spriteManager.loadSprite('platform', 'assets/images/platform.png', 64, 32, 1);
        await this.spriteManager.loadSprite('coin', 'assets/images/coin.png', 16, 16, 8);
        
        // Устанавливаем спрайт персонажу
        if (this.player && this.spriteManager.getSprite('player')) {
            this.player.setSprite(this.spriteManager.getSprite('player'));
        }
    } catch (error) {
        console.warn('Ошибка загрузки спрайтов:', error);
        this.useSprites = false;
    }
}
```

### Шаг 4: Включите использование спрайтов

В конструкторе `Game` установите:

```javascript
this.useSprites = true; // Вместо false
```

## 📐 Формат спрайт-листов

Если у вас спрайт-лист (несколько кадров в одном файле):

```
[Кадр 1][Кадр 2][Кадр 3][Кадр 4]
```

При загрузке укажите:
- `frameWidth` - ширина одного кадра
- `frameHeight` - высота одного кадра  
- `frames` - количество кадров в спрайте

**Пример:**
```javascript
// Спрайт-лист: 4 кадра по 32x32 пикселя
await this.spriteManager.loadSprite('player', 'assets/images/player.png', 32, 32, 4);
```

## 🎨 Рекомендуемые размеры спрайтов

- **Персонаж**: 32x32 или 64x64 пикселя
- **Платформы**: 64x32 или 128x32 пикселя
- **Монеты**: 16x16 или 32x32 пикселя
- **Враги**: 32x32 пикселя

## 🔧 Настройка анимации

Вы можете настроить скорость анимации:

```javascript
const sprite = this.spriteManager.getSprite('player');
sprite.setAnimationSpeed(0.3); // Быстрее
sprite.setAnimationSpeed(0.1); // Медленнее
```

## 📝 Пример полной настройки

```javascript
// В Game.js конструкторе
this.useSprites = true;

// В методе loadSprites()
async loadSprites() {
    try {
        // Загружаем все спрайты
        await Promise.all([
            this.spriteManager.loadSprite('player', 'assets/images/player.png', 32, 32, 4),
            this.spriteManager.loadSprite('platform', 'assets/images/platform.png', 64, 32, 1),
            this.spriteManager.loadSprite('coin', 'assets/images/coin.png', 16, 16, 8),
        ]);
        
        // Настраиваем анимацию персонажа
        const playerSprite = this.spriteManager.getSprite('player');
        if (playerSprite) {
            playerSprite.setAnimationSpeed(0.2);
            this.player.setSprite(playerSprite);
        }
    } catch (error) {
        console.warn('Ошибка загрузки спрайтов:', error);
        this.useSprites = false;
    }
}
```

## ⚠️ Важные замечания

1. **Формат файлов**: Используйте PNG с прозрачностью для лучшего качества
2. **Размер файлов**: Оптимизируйте изображения для веба (используйте TinyPNG или аналоги)
3. **CORS**: При локальной разработке может потребоваться локальный сервер
4. **Fallback**: Если спрайт не загрузится, игра будет использовать стандартную отрисовку

## 🚀 Быстрый старт с Kenney Platformer Pack

1. Скачайте "Platformer Pack" с https://kenney.nl/assets/platformer-pack-redux
2. Распакуйте архив
3. Скопируйте нужные спрайты в `assets/images/`
4. Используйте следующие пути:
   - Персонаж: `assets/images/player.png`
   - Платформа: `assets/images/platform.png`
   - Монета: `assets/images/coin.png`

## 📦 Структура папок

```
2D PLATFORM/
├── assets/
│   └── images/
│       ├── player.png
│       ├── platform.png
│       ├── coin.png
│       └── ...
├── js/
│   ├── Sprite.js
│   └── ...
└── ...
```

---

**Примечание**: Спрайты опциональны. Игра работает и без них, используя простую отрисовку прямоугольниками.

